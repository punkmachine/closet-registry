DATABASE_URL=postgresql://postgres:<пароль>@localhost:5432/closet_registry
ADMIN_TOKEN=
PORT=3000

# Только для docker/docker-compose.yml (postgres + traefik):
POSTGRES_PASSWORD=<пароль>
POSTGRES_DB=closet_registry
DOMAIN=
ACME_EMAIL=
